/*
 * socket_cliente.cpp
 *
 *  Created on: 7 may 2026
 *      Author: nerea.f.m
 */


//Ejemplo de como leer las respuestas del cliente (es la penultima funcin en el procesador)

//GET_RETO_EXTRA — devuelve OK|rol|puesto|dias
/*TipoRol rol = HACKER;
int puesto = 0, dias = 0;
if (strncmp(recvBuff, "OK", 2) == 0) {
    int rol_int = 0;
    sscanf(recvBuff, "OK|%d|%d|%d", &rol_int, &puesto, &dias);
    rol = (TipoRol)rol_int;
}*/
